# Scenario C-001: Explain auth session flow

## Prompt

Explain how `create_session` and `login` in `src/auth.py` interact, and what
happens if an unknown username is passed to `login`.

## Category

C — Explanation / navigation

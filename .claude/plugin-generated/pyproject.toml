[project]
name = "orchestrator-plugin"
version = "1.0.0"
description = "Auto-generated orchestrator plugin"
requires-python = ">=3.8"

dependencies = [
    "pydantic>=2.0",
    "typing_extensions>=4.0"
]

[tool.poetry]
packages = [
    { include = "src" }
]

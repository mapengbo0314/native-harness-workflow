[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sample-py-app"
version = "0.1.0"
dependencies = [
    "jinja2",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
